@model IEnumerable<GCDataTier.Models.ProjectVolunteer>

@{
    ViewBag.Title = "Index";
}

<h2 class="object-title">Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Project.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Volunteer.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Volunteer.LastName)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.IsLead)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Project.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Volunteer.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Volunteer.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsLead)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProjectVolunteerId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProjectVolunteerId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProjectVolunteerId })
        </td>
    </tr>
}

</table>
