@model GCDataTier.Models.SkillLevel

@{
    ViewBag.Title = "Edit";
}

<h2 class="object-title">Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>SkillLevel</legend>

        @Html.HiddenFor(model => model.SkillLevelId)

        <div class="editor-label">
            @Html.LabelFor(model => model.SkillLevelDescription)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SkillLevelDescription)
            @Html.ValidationMessageFor(model => model.SkillLevelDescription)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
